{
  "name": "@thirty/food-db",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Base de données aliments curatée — seed data, parsers, validateurs",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "biome check .",
    "check": "tsc --noEmit",
    "test": "vitest",
    "validate": "tsx src/validate.ts"
  }
}
